name: inversion_ideas
channels:
  - conda-forge
dependencies:
  - python>=3.11
  - pip
  # Requirements
  - numpy>=2.0.0
  - scipy
  - rich
  - simpeg==0.24
  - pymatsolver==0.3.1  # need to pin it for simpeg v0.24.0
  - discretize==0.11
  # Required by notebooks
  - harmonica
  - verde
  - pandas
  # Jupyter
  - jupyter
  # Dev
  - pytest
  - ruff
  - mypy
  - scipy-stubs
